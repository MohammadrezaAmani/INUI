from inui.elements import *
from inui.svg import *

Html(
    data=(
        Body(
            data=(
                P(
                    data=(
                        """{{> scrollhide-nav}}

""",
                    )
                ),
                H3(data=("""Scroll down a bit...then back up""",)),
            )
        ),
    )
)
